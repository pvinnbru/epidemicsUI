import { RunOnCreateDirective } from './run-on-create.directive';

describe('RunOnCreateDirective', () => {
  it('should create an instance', () => {
    const directive = new RunOnCreateDirective();
    expect(directive).toBeTruthy();
  });
});
